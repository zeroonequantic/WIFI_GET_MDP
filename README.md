# WIFI_GET_MDP
Script python qui récupère automatiquement le mot de passe du wifi auquel l'ordinateur est connecté

1. git clone https://github.com/zeroonequantic/WIFI_GET_MDP.git && pip install -r requirements.txt

